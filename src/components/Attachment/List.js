import styled from 'styled-components';

import { rem } from '../../helpers/heleprs';

const List = styled.ul`
   list-style: none;
   margin: 0;
   padding: 0;
   padding: ${rem(15)};
`;

export { List };