import styled from 'styled-components';

import { em } from '../../helpers/heleprs';

const List = styled.ul`
   /* list-style: none;
   margin: 0;
   padding: 0; */
   /* padding: ${em(15)}; */
`;

export { List };