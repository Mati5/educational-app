import styled from 'styled-components';

import { rem } from '../../helpers/heleprs';

const Title= styled.div`
  color: #222;
  font-weight: 600;
  text-align: center;
`;

export { Title };